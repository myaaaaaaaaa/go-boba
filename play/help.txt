
Plays back terminal "animations" from a text file.

This program copies data from stdin to stdout as-is. It pauses on
specific sequences of characters to create a playback effect. This allows
specially prepared plain text files to act as animations.

The program pauses on:
  - Newlines
  - ANSI clear screen escape codes

Flags:
